# Internal development notes (never push)
_internal/

# Submitted separately
paper.pdf
Presentation.pdf

# Claude Code worktrees / agent state (never push)
.claude/

# LaTeX compile artifacts if report sources are built locally
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.synctex.gz
paper/*.toc
paper/*.fls
paper/*.fdb_latexmk
paper/main-blx.bib

# Datasets (too large, download via code)
data/

# Python
.cache/
__pycache__/
*.pyc
*.pyo
*.egg-info/
.eggs/
dist/
build/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Virtual environments
.venv/
venv/
env/

# Raw per-run outputs — keep summary.json tracked, ignore the heavy per-run
# results.json files (each ~10 MB; summary.json carries aggregated metrics)
results/samplewise_nn/k*_n*_ep*_s*/
results/nn_spectral/k*_n*_ep*_s*/
results/full_empirical_ntk/k*_n*_ep*_s*/
results/depth_ablation/d*_k*_n*_ep*_s*/
results/resnet18_compare/rn18m*_n*_ep*_s*/
results/hessian_topeig/k*_n*_ep*_s*/
results/activation_ablation/*_k*_n*_ep*_s*/
logs/
logs_*.txt
